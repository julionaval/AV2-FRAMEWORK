# Backend - Gerenciador de Tarefas (MySQL / Laragon)

## Setup rápido
1. Copie `.env.example` para `.env` e ajuste variáveis (`DB_*`).  
2. (Opcional) set GOOGLE_APPLICATION_CREDENTIALS se quiser autenticação Firebase real.  
3. Instale dependências:
